<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:c="using:Breakpoints.Avalonia.Controls">
    <ControlTheme x:Key="{x:Type c:Breakpoint}"
                  TargetType="c:Breakpoint">
        <Setter Property="Template">
            <ControlTemplate>
                <ContentPresenter Content="{TemplateBinding Content}"
                                  IsVisible="{TemplateBinding Enabled}" />
            </ControlTemplate>
        </Setter>
    </ControlTheme>
</ResourceDictionary>
